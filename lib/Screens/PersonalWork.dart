import 'package:flutter/material.dart';

class PersonalWork extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1000,
      height: 1000,
      color: Colors.blue,
      child: Text("PERSONAL WORK"),
    );
  }
}
