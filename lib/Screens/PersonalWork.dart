import 'package:flutter/material.dart';

class PersonalWork extends StatelessWidget {
  const PersonalWork({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1000,
      height: 1000,
      color: Colors.blue,
      child: const Text("PERSONAL WORK"),
    );
  }
}
