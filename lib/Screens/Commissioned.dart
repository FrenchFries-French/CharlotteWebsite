import 'package:flutter/material.dart';

class Commissioned extends StatelessWidget {
  const Commissioned({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1000,
      height: 1000,
      color: Colors.red,
      child: const Text("COMMISSIONED"),
    );
  }
}
